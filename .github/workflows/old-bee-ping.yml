name: Keep Old Bee Bot and scraper awake

on:
  push:
    branches:
      - main
  schedule:
    - cron: "*/10 * * * *" # Runs every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Services
        run: |
          curl https://ol-bee-bot.onrender.com/
          curl https://puppet-render-latest-n781.onrender.com
